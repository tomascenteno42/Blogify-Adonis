@layout('layout.main');

@section('title')
Blogify
@endsection

@section('content')
<diV>
  <h1>
    Blog feed
  </h1>
  @each(post in posts)
  <div class="blank"></div>
    <div class="posts">
      <h3><a href="">{{ post.title }}</a></h3>
      <span>{{ post.subtitle }}</span>
      <p>{{ post.body }}</p>
      <span>{{ post.user.username }}</span>
    </div>
  @endeach
</div>
@endsection 